#include <iostream>
using namespace std;

int main() {
    int x; cin >> x;
    int tens = x / 10, ones = x % 10;
    cout << tens << "\n" << ones << "\n" << (tens + ones) << "\n" << (tens * ones) << "\n";
    return 0;
}
