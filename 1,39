#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x,y; cin >> x >> y;

    double z = (x + (2 + y)/(x*x)) / (y + 1.0/sqrt(x*x + 10));
    double q = 2.8*sin(x) + fabs(y);

    cout << z << "\n" << q << "\n";
}
