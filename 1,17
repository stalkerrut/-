#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double x1,x2,x3,v0,t,a,m,g,h,R1,R2,alpha,beta,c,ad,bc,ax,bx,cc,kI;
    double m1,m2,r,x;
    cin >> x1>>x2>>x3>>v0>>t>>a>>m>>g>>h>>R1>>R2>>alpha>>beta>>c>>m1>>m2>>r>>x;

    cout << sqrt(x1*x1 + x2*x2) << "\n";              // a)
    cout << x1*x2 + x1*x3 + x2*x3 << "\n";            // б)
    cout << v0*t + (a*t*t)/2.0 << "\n";               // в)
    cout << (m*x*x)/2.0 + m*g*h << "\n";              // г)
    cout << 1.0/R1 + 1.0/R2 << "\n";                  // д)
    cout << m*g*cos(alpha) << "\n";                   // е)
    cout << 2*M_PI*R1 << "\n";                        // ж) (берём R=R1)
    cout << beta*beta - 4*a*c << "\n";                // з) (здесь beta=b)
    cout << (m1*m2)/(r*r) << "\n";                    // и)
    cout << kI*kI*R1 << "\n";                         // к) I^2 R (kI=I)
    cout << fabs(a)*sin(c) << "\n";                   // л) abs*sin
    cout << sqrt(a*a + b*b - 2*a*b*cos(c)) << "\n";   // м) (нужно b — нет, см. ниже)
    return 0;
}
