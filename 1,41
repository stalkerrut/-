#include <iostream>
#include <cmath>
using namespace std;

int main() {
    double e, f, g, h;
    cin >> e >> f >> g >> h;

    double a = sqrt(pow(fabs(e - 3.0 / f), 3.0)) + g;
    double b = sin(e) + pow(cos(h), 2.0);
    double c = (33.0 * g) / (e * f - 3.0);

    cout << a << "\n" << b << "\n" << c << "\n";
    return 0;
}
