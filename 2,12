#include <iostream>
using namespace std;

int main() {
    int x; cin >> x;
    int a = x / 100, b = (x / 10) % 10, c = x % 10;
    cout << c << "\n" << b << "\n" << (a + b + c) << "\n" << (a * b * c) << "\n";
    return 0;
}
