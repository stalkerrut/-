#include <iostream>
#include <cmath>
using namespace std;

int main() {
    long long a,b; cin >> a >> b;
    double ar = (a + b) / 2.0;
    double geo = sqrt((double)a * (double)b);
    cout << ar << "\n" << geo << "\n";
}
