#include <iostream>
using namespace std;

int main() {
    int x; cin >> x;
    int a = x / 1000;
    int b = (x / 100) % 10;
    int c = (x / 10) % 10;
    int d = x % 10;
    cout << (a + b + c + d) << "\n" << (a * b * c * d) << "\n";
    return 0;
}
